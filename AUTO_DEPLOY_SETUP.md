# 🚀 Настройка автодеплоя на Vercel

## ✅ АВТОДЕПЛОЙ УЖЕ ДОЛЖЕН РАБОТАТЬ

Если проект подключен к Vercel через Git, автодеплой включён по умолчанию.

**Как это работает:**
1. Ты делаешь `git push origin main`
2. Vercel автоматически видит изменения
3. Vercel запускает деплой (обычно через 10-30 секунд)
4. Деплой завершается через 2-3 минуты

---

## 🔍 КАК ПРОВЕРИТЬ, ЧТО АВТОДЕПЛОЙ РАБОТАЕТ

### 1. Проверь подключение Git на Vercel

**Действия:**
1. Открой: https://vercel.com/dashboard
2. Найди проект `ai-chat-pro` или `chatedem.com`
3. Открой: Settings → Git
4. Проверь:
   - ✅ Репозиторий подключен?
   - ✅ Ветка: `main` или `master`?
   - ✅ Production Branch: `main`?

**Если репозиторий не подключен:**
- Нажми "Connect Git Repository"
- Выбери GitHub/GitLab/Bitbucket
- Выбери репозиторий
- Vercel автоматически настроит автодеплой

---

### 2. Проверь настройки деплоя

**Действия:**
1. Vercel Dashboard → Settings → General
2. Проверь:
   - **Production Branch:** `main` (или `master`)
   - **Auto-deploy:** должен быть включён

**Если Auto-deploy выключен:**
- Включи его
- Сохрани изменения

---

### 3. Проверь последние деплои

**Действия:**
1. Vercel Dashboard → Deployments
2. Посмотри последние деплои
3. Проверь:
   - Есть ли деплои после `git push`?
   - Статус деплоев (успешные/неуспешные)?

**Если деплои есть** → автодеплой работает! ✅

**Если деплоев нет** → нужно подключить Git (см. выше)

---

## 🚀 КАК ЗАПУСТИТЬ ДЕПЛОЙ СЕЙЧАС

### Вариант 1: Через Git (автоматически)

**Выполни в терминале:**
```bash
cd /Users/sanecek/tema/ai-chat-pro
git add -A
git commit -m "Fix: Improve Sage voice prompt for deeper, wiser responses"
git push origin main
```

**Что произойдёт:**
1. Изменения отправятся в Git
2. Vercel увидит изменения (через 10-30 секунд)
3. Vercel запустит деплой автоматически
4. Деплой завершится через 2-3 минуты

**Проверь:**
- Vercel Dashboard → Deployments
- Должен появиться новый деплой

---

### Вариант 2: Вручную через Vercel Dashboard

**Если автодеплой не работает:**

1. Открой: Vercel Dashboard → Deployments
2. Найди последний деплой
3. Нажми "Redeploy"
4. Подожди 2-3 минуты

---

## ⚙️ НАСТРОЙКИ АВТОДЕПЛОЯ

### Production Branch

**Где:** Settings → General → Production Branch

**Должно быть:** `main` (или `master`)

**Что делает:**
- Деплои из этой ветки идут на продакшен
- Другие ветки создают Preview деплои

---

### Auto-deploy

**Где:** Settings → General → Auto-deploy

**Должно быть:** Включено ✅

**Что делает:**
- Автоматически деплоит при `git push`
- Если выключено → нужно деплоить вручную

---

### Build Command

**Где:** Settings → General → Build Command

**Должно быть:** `npm run build` (или пусто, Vercel определит автоматически)

---

### Output Directory

**Где:** Settings → General → Output Directory

**Должно быть:** `.next` (или пусто для Next.js)

---

## 📋 ЧЕКЛИСТ НАСТРОЙКИ АВТОДЕПЛОЯ

- [ ] Репозиторий подключен к Vercel (Settings → Git)
- [ ] Production Branch = `main` (Settings → General)
- [ ] Auto-deploy включён (Settings → General)
- [ ] Build Command правильный (Settings → General)
- [ ] Последние деплои успешные (Deployments)

---

## 🔧 ЕСЛИ АВТОДЕПЛОЙ НЕ РАБОТАЕТ

### Проблема 1: Репозиторий не подключен

**Решение:**
1. Vercel Dashboard → Settings → Git
2. Нажми "Connect Git Repository"
3. Выбери репозиторий
4. Vercel настроит автодеплой автоматически

---

### Проблема 2: Неправильная ветка

**Решение:**
1. Vercel Dashboard → Settings → General
2. Проверь Production Branch
3. Если неправильная → измени на `main`
4. Сохрани

---

### Проблема 3: Auto-deploy выключен

**Решение:**
1. Vercel Dashboard → Settings → General
2. Включи "Auto-deploy"
3. Сохрани

---

### Проблема 4: Ошибки сборки

**Решение:**
1. Vercel Dashboard → Deployments
2. Открой неуспешный деплой
3. Посмотри логи ошибок
4. Исправь ошибки в коде
5. Запушь снова

---

## 🚀 БЫСТРЫЙ ТЕСТ АВТОДЕПЛОЯ

**1. Создай тестовый коммит:**
```bash
cd /Users/sanecek/tema/ai-chat-pro
git commit --allow-empty -m "Test: Auto-deploy"
git push origin main
```

**2. Открой Vercel Dashboard:**
- Deployments
- Подожди 30 секунд
- Должен появиться новый деплой

**3. Если деплой появился** → автодеплой работает! ✅

**4. Если деплой не появился** → проверь настройки (см. выше)

---

## 💡 РЕКОМЕНДАЦИИ

**Для стабильного автодеплоя:**

1. **Всегда пуши в `main`** → автоматический деплой на продакшен
2. **Используй другие ветки** → Preview деплои для тестирования
3. **Проверяй деплои** → следи за статусом в Vercel Dashboard
4. **Исправляй ошибки** → если деплой неуспешный, исправь и запушь снова

---

**Автодеплой должен работать автоматически при `git push`! 🚀**

