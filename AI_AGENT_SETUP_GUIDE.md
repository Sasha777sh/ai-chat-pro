# 🤖 Инструкция для ИИ-агента по настройке проекта EDEM Intelligence

## 📋 Контекст проекта

**Проект:** EDEM Intelligence — AI чат-сервис с 5 голосами ИИ  
**Статус:** Код полностью готов, сборка успешна  
**Задача:** Настроить Supabase, Stripe и Vercel для деплоя  
**Домен/URL:** Тебе нужно будет узнать текущий URL проекта в Vercel

---

## 🎯 Цель

Настроить:
1. ✅ Миграцию базы данных в Supabase
2. ✅ Продукты и цены в Stripe Dashboard
3. ✅ Webhook в Stripe
4. ✅ Переменные окружения в Vercel

**Время выполнения:** ~15-20 минут

---

## 📝 ШАГ 1: Подготовка и доступы

### 1.1 Собери информацию

Мне нужно:
- ✅ URL проекта в Vercel (например: `https://your-project.vercel.app`)
- ✅ Доступ к Supabase Dashboard (URL проекта)
- ✅ Доступ к Stripe Dashboard (account ID)
- ✅ Доступ к Vercel Dashboard (project name)

### 1.2 Что проверить

1. Проект уже задеплоен на Vercel? (проверь через Vercel Dashboard)
2. Есть доступ к Supabase Dashboard?
3. Есть доступ к Stripe Dashboard?

**Действие:** Зайди в Vercel Dashboard и найди URL проекта. Запиши его.

---

## 📝 ШАГ 2: Supabase — Применение миграции

### 2.1 Зайди в Supabase Dashboard

1. Открой браузер
2. Зайди на `https://supabase.com/dashboard`
3. Выбери проект EDEM Intelligence (или создай новый, если нет)

### 2.2 Открой SQL Editor

1. В левом меню найди **"SQL Editor"**
2. Нажми **"New query"**

### 2.3 Примени миграцию

1. Открой файл: `ai-chat-pro/supabase/migrations/003_voice_and_billing.sql`
2. Скопируй ВЕСЬ код из этого файла
3. Вставь в SQL Editor в Supabase
4. Нажми **"Run"** или `Ctrl+Enter`

### 2.4 Проверь результат

После выполнения миграции должно появиться сообщение:
- ✅ "Success. No rows returned" или похожее

**Что делает миграция:**
- Добавляет `voice_id` в таблицу `chat_sessions`
- Создаёт таблицу `billing_subscriptions`
- Обновляет констрейнт тарифов (free, basic, plus, pro)

### 2.5 Проверь созданные таблицы

1. В левом меню найди **"Table Editor"**
2. Проверь наличие таблицы `billing_subscriptions`
3. Проверь, что в `chat_sessions` есть колонка `voice_id`

**Если ошибка:** Скопируй текст ошибки и сообщи мне.

---

## 📝 ШАГ 3: Stripe — Создание продуктов и цен

### 3.1 Зайди в Stripe Dashboard

1. Открой браузер
2. Зайди на `https://dashboard.stripe.com`
3. ВАЖНО: Проверь, что ты в **Test mode** (переключатель вверху справа)

### 3.2 Создай продукт "Basic" ($15/мес)

1. В левом меню найди **"Products"**
2. Нажми **"+ Add product"**
3. Заполни:
   - **Name:** `Basic`
   - **Description:** `Глубокая поддержка через Голос Живого`
   - **Pricing model:** `Standard pricing`
   - **Price:** `15.00`
   - **Billing period:** `Monthly`
   - **Currency:** `USD`
4. Нажми **"Save product"**
5. **СКОПИРУЙ Price ID** (начинается с `price_...`) — он понадобится позже!
   - Найди его в разделе "Pricing" продукта
   - Выглядит как: `price_1AbCdEfGhIjKlMn`

### 3.3 Создай продукт "Plus" ($29/мес)

1. Нажми **"+ Add product"**
2. Заполни:
   - **Name:** `Plus`
   - **Description:** `Живое + Зеркало + Ребёнок`
   - **Pricing model:** `Standard pricing`
   - **Price:** `29.00`
   - **Billing period:** `Monthly`
   - **Currency:** `USD`
3. Нажми **"Save product"**
4. **СКОПИРУЙ Price ID** (`price_...`)

### 3.4 Создай продукт "Pro" ($49/мес)

1. Нажми **"+ Add product"**
2. Заполни:
   - **Name:** `Pro`
   - **Description:** `Вся палитра голосов`
   - **Pricing model:** `Standard pricing`
   - **Price:** `49.00`
   - **Billing period:** `Monthly`
   - **Currency:** `USD`
3. Нажми **"Save product"**
4. **СКОПИРУЙ Price ID** (`price_...`)

### 3.5 Запиши все Price ID

У тебя должно быть 3 Price ID:
- `STRIPE_PRICE_BASIC` = `price_...` (из продукта Basic)
- `STRIPE_PRICE_PLUS` = `price_...` (из продукта Plus)
- `STRIPE_PRICE_PRO` = `price_...` (из продукта Pro)

**Сохрани их — они понадобятся в ШАГ 5!**

---

## 📝 ШАГ 4: Stripe — Настройка Webhook

### 4.1 Открой Webhooks в Stripe

1. В левом меню Stripe Dashboard найди **"Developers"**
2. Нажми **"Webhooks"**
3. Нажми **"+ Add endpoint"**

### 4.2 Настрой endpoint

1. **Endpoint URL:** 
   ```
   https://YOUR-VERCEL-URL/api/stripe/webhook
   ```
   Замени `YOUR-VERCEL-URL` на реальный URL из Vercel (например: `https://edem-intelligence.vercel.app`)

2. **Description:** `EDEM Intelligence - Subscription webhooks`

3. **Events to send:** Выбери эти события:
   - ✅ `checkout.session.completed`
   - ✅ `customer.subscription.updated`
   - ✅ `customer.subscription.deleted`

4. Нажми **"Add endpoint"**

### 4.3 Получи Webhook Secret

1. После создания endpoint, нажми на него
2. Найди раздел **"Signing secret"**
3. Нажми **"Reveal"** рядом с секретом
4. **СКОПИРУЙ Webhook Secret** (начинается с `whsec_...`)
   - Выглядит как: `whsec_1AbCdEfGhIjKlMn`

**Сохрани его — он понадобится в ШАГ 5!**

### 4.4 Проверь webhook

1. В разделе webhook нажми на только что созданный endpoint
2. Проверь, что статус **"Enabled"** (зелёный)
3. Проверь, что URL правильный

**Если URL неправильный:** Отредактируй endpoint и исправь URL.

---

## 📝 ШАГ 5: Vercel — Добавление переменных окружения

### 5.1 Зайди в Vercel Dashboard

1. Открой браузер
2. Зайди на `https://vercel.com/dashboard`
3. Найди проект `ai-chat-pro` (или как он называется)

### 5.2 Открой Settings проекта

1. Нажми на проект
2. Вверху найди вкладку **"Settings"**
3. В левом меню найди **"Environment Variables"**

### 5.3 Добавь переменные для Stripe

**ВАЖНО:** Все переменные добавляй для **Production, Preview, Development** (выбери все три чекбокса)

#### 5.3.1 STRIPE_SECRET_KEY

1. Нажми **"+ Add New"**
2. **Key:** `STRIPE_SECRET_KEY`
3. **Value:** Твой Stripe Secret Key
   - Найди его в Stripe Dashboard: **Developers** → **API keys**
   - Копируй **Secret key** (начинается с `sk_test_...` или `sk_live_...`)
   - ВАЖНО: Используй Test key (`sk_test_...`) для тестирования
4. Выбери все окружения: ✅ Production, ✅ Preview, ✅ Development
5. Нажми **"Save"**

#### 5.3.2 STRIPE_WEBHOOK_SECRET

1. Нажми **"+ Add New"**
2. **Key:** `STRIPE_WEBHOOK_SECRET`
3. **Value:** Webhook Secret из ШАГ 4.3 (начинается с `whsec_...`)
4. Выбери все окружения: ✅ Production, ✅ Preview, ✅ Development
5. Нажми **"Save"**

#### 5.3.3 STRIPE_PRICE_BASIC

1. Нажми **"+ Add New"**
2. **Key:** `STRIPE_PRICE_BASIC`
3. **Value:** Price ID продукта Basic из ШАГ 3.2 (начинается с `price_...`)
4. Выбери все окружения: ✅ Production, ✅ Preview, ✅ Development
5. Нажми **"Save"**

#### 5.3.4 STRIPE_PRICE_PLUS

1. Нажми **"+ Add New"**
2. **Key:** `STRIPE_PRICE_PLUS`
3. **Value:** Price ID продукта Plus из ШАГ 3.3 (начинается с `price_...`)
4. Выбери все окружения: ✅ Production, ✅ Preview, ✅ Development
5. Нажми **"Save"**

#### 5.3.5 STRIPE_PRICE_PRO

1. Нажми **"+ Add New"**
2. **Key:** `STRIPE_PRICE_PRO`
3. **Value:** Price ID продукта Pro из ШАГ 3.4 (начинается с `price_...`)
4. Выбери все окружения: ✅ Production, ✅ Preview, ✅ Development
5. Нажми **"Save"**

#### 5.3.6 NEXT_PUBLIC_APP_URL

1. Нажми **"+ Add New"**
2. **Key:** `NEXT_PUBLIC_APP_URL`
3. **Value:** Твой Vercel URL (например: `https://edem-intelligence.vercel.app`)
4. Выбери все окружения: ✅ Production, ✅ Preview, ✅ Development
5. Нажми **"Save"**

### 5.4 Проверь существующие переменные

Убедись, что уже есть эти переменные:
- ✅ `NEXT_PUBLIC_SUPABASE_URL`
- ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- ✅ `SUPABASE_SERVICE_ROLE_KEY`
- ✅ `OPENAI_API_KEY`

**Если какой-то нет — добавь её тоже!**

### 5.5 Передеплой проект

1. После добавления всех переменных
2. В Vercel Dashboard нажми **"Deployments"**
3. Найди последний deployment
4. Нажми **"..."** (три точки) → **"Redeploy"**
5. Или сделай новый commit в Git, это автоматически передеплоит

**ВАЖНО:** Переменные окружения применяются только после нового деплоя!

---

## 📝 ШАГ 6: Проверка настройки

### 6.1 Проверь Supabase

1. Зайди в Supabase Dashboard → Table Editor
2. Проверь наличие таблицы `billing_subscriptions`
3. Проверь, что в `chat_sessions` есть колонка `voice_id`

✅ **Если видишь таблицы — миграция применена успешно!**

### 6.2 Проверь Stripe продукты

1. Зайди в Stripe Dashboard → Products
2. Убедись, что видишь 3 продукта: Basic, Plus, Pro
3. У каждого продукта должен быть Price ID (начинается с `price_...`)

✅ **Если видишь 3 продукта — продукты созданы успешно!**

### 6.3 Проверь Stripe Webhook

1. Зайди в Stripe Dashboard → Developers → Webhooks
2. Найди созданный webhook endpoint
3. Проверь:
   - ✅ URL правильный (твой Vercel URL + `/api/stripe/webhook`)
   - ✅ Статус "Enabled" (зелёный)
   - ✅ Подписаны на события: `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`

✅ **Если всё зелёное — webhook настроен успешно!**

### 6.4 Проверь Vercel переменные

1. Зайди в Vercel Dashboard → Settings → Environment Variables
2. Проверь наличие всех переменных:
   - ✅ `STRIPE_SECRET_KEY`
   - ✅ `STRIPE_WEBHOOK_SECRET`
   - ✅ `STRIPE_PRICE_BASIC`
   - ✅ `STRIPE_PRICE_PLUS`
   - ✅ `STRIPE_PRICE_PRO`
   - ✅ `NEXT_PUBLIC_APP_URL`
   - ✅ `NEXT_PUBLIC_SUPABASE_URL`
   - ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - ✅ `SUPABASE_SERVICE_ROLE_KEY`
   - ✅ `OPENAI_API_KEY`

✅ **Если все переменные есть — переменные добавлены успешно!**

### 6.5 Тестовый запуск (опционально)

1. Зайди на твой Vercel URL (например: `https://edem-intelligence.vercel.app`)
2. Попробуй зарегистрироваться
3. Попробуй создать сессию чата
4. Попробуй перейти на `/billing`
5. Попробуй нажать "Оформить подписку" (но НЕ завершай оплату в Stripe Test mode)

**Если страницы открываются — проект работает! ✅**

---

## 🎯 Итоговый чеклист

Перед тем как сообщить о завершении, проверь:

- [ ] ✅ Миграция применена в Supabase
- [ ] ✅ Таблица `billing_subscriptions` создана
- [ ] ✅ Колонка `voice_id` есть в `chat_sessions`
- [ ] ✅ 3 продукта созданы в Stripe (Basic, Plus, Pro)
- [ ] ✅ У каждого продукта есть Price ID (`price_...`)
- [ ] ✅ Webhook создан в Stripe
- [ ] ✅ Webhook URL правильный (Vercel URL + `/api/stripe/webhook`)
- [ ] ✅ Webhook подписан на нужные события
- [ ] ✅ Webhook Secret скопирован (`whsec_...`)
- [ ] ✅ Все переменные добавлены в Vercel
- [ ] ✅ Проект передеплоен в Vercel
- [ ] ✅ Сайт открывается и работает

---

## ⚠️ Частые ошибки

### Ошибка: "Table already exists"
**Решение:** Таблица уже создана, это нормально. Продолжай дальше.

### Ошибка: "Invalid webhook URL"
**Решение:** Проверь, что URL правильный и проект задеплоен на Vercel.

### Ошибка: "Price ID not found"
**Решение:** Убедись, что скопировал правильный Price ID из Stripe (должен начинаться с `price_...`).

### Ошибка: "Environment variable not found"
**Решение:** Убедись, что добавил переменную для всех окружений (Production, Preview, Development) и передеплоил проект.

### Ошибка: "Webhook signature verification failed"
**Решение:** Проверь, что `STRIPE_WEBHOOK_SECRET` правильный (должен начинаться с `whsec_...`).

---

## 📞 Если что-то пошло не так

Если встречаешь ошибку:

1. **Скопируй точный текст ошибки**
2. **Укажи, на каком шаге произошла ошибка**
3. **Сделай скриншот (если возможно)**
4. **Сообщи мне** — я помогу исправить

---

## ✅ Готово!

После выполнения всех шагов проект будет полностью настроен и готов к использованию.

**Последнее:** Сообщи мне, что всё готово, и пришли список всех созданных Price ID и Webhook Secret (для проверки).

---

**Удачи! 🚀**

