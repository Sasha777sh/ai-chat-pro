# 🔧 Задача: Исправить дизайн на localhost

## Проблема
На localhost дизайн не отображается — "беспатновая верстка", стили не применяются.

## ✅ Что уже исправлено
- Обновлён синтаксис Tailwind CSS v4 в `globals.css`:
  - Было: `@tailwind base; @tailwind components; @tailwind utilities;`
  - Стало: `@import "tailwindcss";`

## 📝 Что нужно сделать

### ШАГ 1: Перезапустить dev сервер

1. Останови текущий dev сервер (Ctrl+C)
2. Очисти кеш Next.js:
   ```bash
   rm -rf .next
   ```
3. Перезапусти dev сервер:
   ```bash
   npm run dev
   ```

### ШАГ 2: Проверить в браузере

1. Открой: http://localhost:3000
2. Обнови страницу (Ctrl+Shift+R или Cmd+Shift+R)
3. Проверь, что:
   - Фон тёмный (#0C0C0E)
   - Текст светлый (#F1F1F2)
   - Кнопки и карточки имеют стили EDEM

### ШАГ 3: Если не помогло

1. Проверь консоль браузера (F12 → Console):
   - Есть ли ошибки?
   - Загружаются ли CSS файлы?

2. Проверь Network tab:
   - Загружается ли `globals.css`?
   - Какой статус у файла?

3. Если всё равно не работает:
   ```bash
   # Полная очистка и переустановка
   rm -rf .next node_modules package-lock.json
   npm install
   npm run dev
   ```

## 🎯 Ожидаемый результат

После перезапуска:
- ✅ Тёмный фон (#0C0C0E)
- ✅ Светлый текст (#F1F1F2)
- ✅ Стилизованные кнопки и карточки
- ✅ Цвета EDEM применяются (bg-edem-dark, text-edem-main и т.д.)

## ⚠️ Важно

**Tailwind CSS v4** использует новый синтаксис:
- ❌ Старый: `@tailwind base;`
- ✅ Новый: `@import "tailwindcss";`

Это уже исправлено в коде, но нужно **перезапустить dev сервер**, чтобы изменения применились.

---

**Начни с ШАГА 1 — просто перезапусти dev сервер!**

