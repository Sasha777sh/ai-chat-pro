# 🤖 Инструкция для ИИ-агента по настройке проекта EDEM Intelligence (ЮKassa)

## 📋 Контекст проекта

**Проект:** EDEM Intelligence — AI чат-сервис с 5 голосами ИИ  
**Платежи:** ЮKassa (Россия)  
**Статус:** Код полностью готов, сборка успешна  
**Задача:** Настроить Supabase и ЮKassa для деплоя  
**Домен/URL:** Тебе нужно будет узнать текущий URL проекта в Vercel

---

## 🎯 Цель

Настроить:
1. ✅ Миграцию базы данных в Supabase
2. ✅ Ключи ЮKassa (Shop ID и Secret Key)
3. ✅ Webhook в ЮKassa
4. ✅ Переменные окружения в Vercel

**Время выполнения:** ~10-15 минут

---

## 📝 ШАГ 1: Подготовка и доступы

### 1.1 Собери информацию

Мне нужно:
- ✅ URL проекта в Vercel (например: `https://your-project.vercel.app`)
- ✅ Доступ к Supabase Dashboard (URL проекта)
- ✅ Доступ к ЮKassa Dashboard (личный кабинет)
- ✅ Доступ к Vercel Dashboard (project name)

### 1.2 Что проверить

1. Проект уже задеплоен на Vercel? (проверь через Vercel Dashboard)
2. Есть доступ к Supabase Dashboard?
3. Есть доступ к ЮKassa Dashboard?

**Действие:** Зайди в Vercel Dashboard и найди URL проекта. Запиши его.

---

## 📝 ШАГ 2: Supabase — Применение миграции

### 2.1 Зайди в Supabase Dashboard

1. Открой браузер
2. Зайди на `https://supabase.com/dashboard`
3. Выбери проект EDEM Intelligence (или создай новый, если нет)

### 2.2 Открой SQL Editor

1. В левом меню найди **"SQL Editor"**
2. Нажми **"New query"**

### 2.3 Примени миграцию

1. Открой файл: `ai-chat-pro/supabase/migrations/003_voice_and_billing.sql`
2. Скопируй ВЕСЬ код из этого файла
3. Вставь в SQL Editor в Supabase
4. Нажми **"Run"** или `Ctrl+Enter`

### 2.4 Проверь результат

После выполнения миграции должно появиться сообщение:
- ✅ "Success. No rows returned" или похожее

**Что делает миграция:**
- Добавляет `voice_id` в таблицу `chat_sessions`
- Создаёт таблицу `billing_subscriptions`
- Обновляет констрейнт тарифов (free, basic, plus, pro)

### 2.5 Проверь созданные таблицы

1. В левом меню найди **"Table Editor"**
2. Проверь наличие таблицы `billing_subscriptions`
3. Проверь, что в `chat_sessions` есть колонка `voice_id`

**Если ошибка:** Скопируй текст ошибки и сообщи мне.

---

## 📝 ШАГ 3: ЮKassa — Получение ключей

### 3.1 Зайди в ЮKassa Dashboard

1. Открой браузер
2. Зайди на `https://yookassa.ru/my`
3. Войди в личный кабинет

**Если нет аккаунта:**
1. Зайди на `https://yookassa.ru/join`
2. Зарегистрируйся
3. Создай магазин

### 3.2 Получи ключи

1. В личном кабинете найди **"Настройки магазина"** или **"Настройки"**
2. Найди раздел **"Ключи API"** или **"API ключи"**
3. Скопируй два ключа:
   - **Shop ID** (число, например: `123456` или `1163961`)
   - **Secret Key** (строка, начинается с `test_...` или `live_...`)

**ВАЖНО:** Для тестирования используй **тестовые ключи** (начинаются с `test_`).  
Для продакшена нужны **реальные ключи** (начинаются с `live_`).

### 3.3 Запиши ключи

У тебя должно быть:
- `YK_SHOP_ID` = число (например: `1163961`)
- `YK_SECRET_KEY` = строка (например: `live_SY4gLP1faET4ZUupEm4QhY-ixvgjhOqvdbqzJ9sgrGU`)

**Сохрани их — они понадобятся в ШАГ 5!**

---

## 📝 ШАГ 4: ЮKassa — Настройка Webhook

### 4.1 Открой настройки уведомлений

1. В ЮKassa Dashboard найди **"Настройки"**
2. Найди раздел **"Уведомления"** или **"Webhooks"**

### 4.2 Добавь webhook endpoint

1. Нажми **"Добавить URL"** или **"Создать webhook"**
2. **Webhook URL:**
   ```
   https://YOUR-VERCEL-URL/api/yookassa/webhook
   ```
   Замени `YOUR-VERCEL-URL` на реальный URL из Vercel (например: `https://edem-intelligence.vercel.app`)

3. **Описание:** `EDEM Intelligence - Payment webhooks`

4. **События для отправки:** Выбери:
   - ✅ `payment.succeeded` — успешная оплата
   - (Опционально) `payment.canceled` — отмена оплаты

5. Нажми **"Сохранить"** или **"Создать"**

### 4.3 Проверь webhook

1. В списке webhooks найди только что созданный endpoint
2. Проверь, что статус **"Активен"** или **"Enabled"** (зелёный)
3. Проверь, что URL правильный

**Если URL неправильный:** Отредактируй webhook и исправь URL.

---

## 📝 ШАГ 5: Vercel — Добавление переменных окружения

### 5.1 Зайди в Vercel Dashboard

1. Открой браузер
2. Зайди на `https://vercel.com/dashboard`
3. Найди проект `ai-chat-pro` (или как он называется)

### 5.2 Открой Settings проекта

1. Нажми на проект
2. Вверху найди вкладку **"Settings"**
3. В левом меню найди **"Environment Variables"**

### 5.3 Добавь переменные для ЮKassa

**ВАЖНО:** Все переменные добавляй для **Production, Preview, Development** (выбери все три чекбокса)

#### 5.3.1 YK_SHOP_ID

1. Нажми **"+ Add New"**
2. **Key:** `YK_SHOP_ID`
3. **Value:** Shop ID из ШАГ 3.2 (число, например: `1163961`)
4. Выбери все окружения: ✅ Production, ✅ Preview, ✅ Development
5. Нажми **"Save"**

#### 5.3.2 YK_SECRET_KEY

1. Нажми **"+ Add New"**
2. **Key:** `YK_SECRET_KEY`
3. **Value:** Secret Key из ШАГ 3.2 (строка, начинается с `test_...` или `live_...`)
4. Выбери все окружения: ✅ Production, ✅ Preview, ✅ Development
5. Нажми **"Save"**

#### 5.3.3 NEXT_PUBLIC_APP_URL

1. Нажми **"+ Add New"**
2. **Key:** `NEXT_PUBLIC_APP_URL`
3. **Value:** Твой Vercel URL (например: `https://edem-intelligence.vercel.app`)
4. Выбери все окружения: ✅ Production, ✅ Preview, ✅ Development
5. Нажми **"Save"**

### 5.4 Проверь существующие переменные

Убедись, что уже есть эти переменные:
- ✅ `NEXT_PUBLIC_SUPABASE_URL`
- ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- ✅ `SUPABASE_SERVICE_ROLE_KEY`
- ✅ `OPENAI_API_KEY`

**Если какой-то нет — добавь её тоже!**

### 5.5 Передеплой проект

1. После добавления всех переменных
2. В Vercel Dashboard нажми **"Deployments"**
3. Найди последний deployment
4. Нажми **"..."** (три точки) → **"Redeploy"**
5. Или сделай новый commit в Git, это автоматически передеплоит

**ВАЖНО:** Переменные окружения применяются только после нового деплоя!

---

## 📝 ШАГ 6: Проверка настройки

### 6.1 Проверь Supabase

1. Зайди в Supabase Dashboard → Table Editor
2. Проверь наличие таблицы `billing_subscriptions`
3. Проверь, что в `chat_sessions` есть колонка `voice_id`

✅ **Если видишь таблицы — миграция применена успешно!**

### 6.2 Проверь ЮKassa ключи

1. Зайди в ЮKassa Dashboard → Настройки → Ключи API
2. Убедись, что видишь:
   - ✅ Shop ID (число)
   - ✅ Secret Key (строка, начинается с `test_...` или `live_...`)

✅ **Если видишь ключи — они получены успешно!**

### 6.3 Проверь ЮKassa Webhook

1. Зайди в ЮKassa Dashboard → Настройки → Уведомления (или Webhooks)
2. Найди созданный webhook endpoint
3. Проверь:
   - ✅ URL правильный (твой Vercel URL + `/api/yookassa/webhook`)
   - ✅ Статус "Активен" (зелёный)
   - ✅ Подписаны на событие: `payment.succeeded`

✅ **Если всё зелёное — webhook настроен успешно!**

### 6.4 Проверь Vercel переменные

1. Зайди в Vercel Dashboard → Settings → Environment Variables
2. Проверь наличие всех переменных:
   - ✅ `YK_SHOP_ID`
   - ✅ `YK_SECRET_KEY`
   - ✅ `NEXT_PUBLIC_APP_URL`
   - ✅ `NEXT_PUBLIC_SUPABASE_URL`
   - ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - ✅ `SUPABASE_SERVICE_ROLE_KEY`
   - ✅ `OPENAI_API_KEY`

✅ **Если все переменные есть — переменные добавлены успешно!**

### 6.5 Тестовый запуск (опционально)

1. Зайди на твой Vercel URL (например: `https://edem-intelligence.vercel.app`)
2. Попробуй зарегистрироваться
3. Попробуй создать сессию чата
4. Попробуй перейти на `/billing`
5. Попробуй нажать "Оформить подписку" на любом тарифе
6. Должна открыться страница оплаты ЮKassa

**Если страница оплаты открывается — проект работает! ✅**

**Для тестовой оплаты используй:**
- Номер карты: `5555 5555 5555 4444`
- Срок: любая будущая дата
- CVC: любые 3 цифры

---

## 🎯 Итоговый чеклист

Перед тем как сообщить о завершении, проверь:

- [ ] ✅ Миграция применена в Supabase
- [ ] ✅ Таблица `billing_subscriptions` создана
- [ ] ✅ Колонка `voice_id` есть в `chat_sessions`
- [ ] ✅ Получены ключи ЮKassa (Shop ID и Secret Key)
- [ ] ✅ Webhook создан в ЮKassa
- [ ] ✅ Webhook URL правильный (Vercel URL + `/api/yookassa/webhook`)
- [ ] ✅ Webhook подписан на событие `payment.succeeded`
- [ ] ✅ Все переменные добавлены в Vercel
- [ ] ✅ Проект передеплоен в Vercel
- [ ] ✅ Сайт открывается и работает
- [ ] ✅ Страница `/billing` отображает тарифы
- [ ] ✅ Кнопка "Оформить подписку" открывает страницу оплаты ЮKassa

---

## 💰 Тарифы и цены в ЮKassa

**Тарифы проекта:**
- **Basic** — 1500₽/мес (доступ к голосу Живого)
- **Plus** — 2900₽/мес (доступ к голосам: Живой, Зеркало, Ребёнок)
- **Pro** — 4900₽/мес (доступ ко всем 5 голосам)

**Цены настроены в коде** (`src/app/api/yookassa/checkout/route.ts`), их можно изменить позже.

---

## ⚠️ Частые ошибки

### Ошибка: "Table already exists"
**Решение:** Таблица уже создана, это нормально. Продолжай дальше.

### Ошибка: "Invalid webhook URL"
**Решение:** Проверь, что URL правильный, начинается с `https://`, и проект задеплоен на Vercel.

### Ошибка: "YK_SHOP_ID not found"
**Решение:** Убедись, что добавил переменную `YK_SHOP_ID` в Vercel и передеплоил проект.

### Ошибка: "YK_SECRET_KEY not found"
**Решение:** Убедись, что добавил переменную `YK_SECRET_KEY` в Vercel и передеплоил проект.

### Ошибка: "Webhook not received"
**Решение:** Проверь, что webhook URL правильный, доступен по HTTPS, и событие `payment.succeeded` подписано.

### Ошибка: "Payment failed"
**Решение:** Проверь, что используешь правильные ключи (test для теста, live для продакшена).

---

## 📞 Если что-то пошло не так

Если встречаешь ошибку:

1. **Скопируй точный текст ошибки**
2. **Укажи, на каком шаге произошла ошибка**
3. **Сделай скриншот (если возможно)**
4. **Сообщи мне** — я помогу исправить

---

## ✅ Готово!

После выполнения всех шагов проект будет полностью настроен и готов к использованию.

**Последнее:** Сообщи мне, что всё готово, и пришли:
- ✅ Shop ID (для проверки)
- ✅ URL проекта в Vercel
- ✅ Подтверждение, что webhook настроен

**Удачи! 🚀**

