# 🤖 ЗАДАЧА ДЛЯ ИИ-АГЕНТА: Собрать переменные окружения автоматически

## 🎯 ЦЕЛЬ

Автоматически собрать все переменные окружения из:
- Supabase Dashboard
- OpenAI Dashboard
- YooKassa Dashboard (если нужно)
- И других источников

И создать файл для импорта в Vercel.

---

## 📋 ЧТО НУЖНО СОБРАТЬ

### 1. Supabase (обязательно)
- `NEXT_PUBLIC_SUPABASE_URL` - URL проекта
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` - anon/public key
- `SUPABASE_SERVICE_ROLE_KEY` - service_role key

### 2. OpenAI (обязательно)
- `OPENAI_API_KEY` - API ключ

### 3. YooKassa (опционально, для оплаты)
- `YK_SHOP_ID` - Shop ID
- `YK_SECRET_KEY` - Secret key

### 4. App URL
- `NEXT_PUBLIC_APP_URL` - должен быть `https://chatedem.com`

---

## 🚀 ПЛАН ДЕЙСТВИЙ

### ШАГ 1: Собрать переменные из Supabase

**Действия:**
1. Открой: https://supabase.com/dashboard
2. Войди в аккаунт (если нужно)
3. Найди проект с URL: `https://jgnnrdrqzcwnhuuvhlfo.supabase.co`
   - Или найди проект по названию
4. Открой: Settings → API
5. Скопируй:
   - **Project URL** → это `NEXT_PUBLIC_SUPABASE_URL`
   - **anon/public key** → это `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role key** → это `SUPABASE_SERVICE_ROLE_KEY`

**Проверка:**
- Anon key должен содержать `"role":"anon"` в JWT
- Service role key должен содержать `"role":"service_role"` в JWT
- Проверь на https://jwt.io

---

### ШАГ 2: Собрать переменные из OpenAI

**Действия:**
1. Открой: https://platform.openai.com/api-keys
2. Войди в аккаунт (если нужно)
3. Найди существующий API ключ или создай новый
4. Скопируй ключ → это `OPENAI_API_KEY`

**Проверка:**
- Ключ должен начинаться с `sk-proj-` или `sk-`
- Не должен быть пустым

---

### ШАГ 3: Собрать переменные из YooKassa (если нужно)

**Действия:**
1. Открой: https://yookassa.ru/developers
2. Войди в аккаунт (если нужно)
3. Найди Shop ID и Secret Key
4. Скопируй:
   - **Shop ID** → это `YK_SHOP_ID`
   - **Secret Key** → это `YK_SECRET_KEY`

**Проверка:**
- Если оплата не нужна, можно пропустить

---

### ШАГ 4: Создать файл для импорта

**Действия:**
1. Создай файл `vercel-env-collected.txt` со следующим содержимым:

```bash
NEXT_PUBLIC_SUPABASE_URL=<скопированный URL из Supabase>
NEXT_PUBLIC_SUPABASE_ANON_KEY=<скопированный anon key>
SUPABASE_SERVICE_ROLE_KEY=<скопированный service role key>
OPENAI_API_KEY=<скопированный API ключ>
NEXT_PUBLIC_APP_URL=https://chatedem.com
```

2. Если есть YooKassa:
```bash
YK_SHOP_ID=<скопированный Shop ID>
YK_SECRET_KEY=<скопированный Secret Key>
```

3. Сохрани файл

---

### ШАГ 5: Проверить переменные

**Действия:**
1. Проверь, что все переменные не пустые
2. Проверь anon key на https://jwt.io (должен содержать `"role":"anon"`)
3. Проверь service role key на https://jwt.io (должен содержать `"role":"service_role"`)
4. Проверь, что `NEXT_PUBLIC_APP_URL` = `https://chatedem.com` (не localhost!)

---

### ШАГ 6: Создать отчёт

**Создай файл:** `ENV_COLLECTION_REPORT.md`

**Содержание:**
1. **Собранные переменные:**
   - Список всех переменных (без значений, только названия)
   - Статус (собрано/не собрано)

2. **Источники:**
   - Откуда взяты переменные
   - Ссылки на дашборды

3. **Проверка:**
   - Результаты проверки ключей
   - Найденные проблемы (если есть)

4. **Файл для импорта:**
   - Путь к файлу `vercel-env-collected.txt`
   - Инструкция по импорту

---

## 🔧 ИНСТРУМЕНТЫ ДЛЯ АВТОМАТИЗАЦИИ

### Используй браузерные инструменты:

1. **browser_navigate** - для открытия страниц
2. **browser_snapshot** - для получения содержимого страницы
3. **browser_click** - для кликов по элементам
4. **browser_type** - для ввода текста
5. **browser_evaluate** - для выполнения JavaScript на странице

### Пример использования:

```javascript
// Открыть Supabase Dashboard
browser_navigate("https://supabase.com/dashboard")

// Получить содержимое страницы
browser_snapshot()

// Найти и кликнуть на проект
browser_click(element="Проект jgnnrdrqzcwnhuuvhlfo", ref="...")

// Открыть Settings → API
browser_click(element="Settings", ref="...")
browser_click(element="API", ref="...")

// Получить ключи через JavaScript
browser_evaluate(function="() => {
  // Найти элементы с ключами
  const anonKey = document.querySelector('[data-key-type=\"anon\"]')?.textContent;
  const serviceKey = document.querySelector('[data-key-type=\"service_role\"]')?.textContent;
  return { anonKey, serviceKey };
}")
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Безопасность:**
   - Не публикуй собранные ключи в отчёте
   - Храни их только в файле `vercel-env-collected.txt`
   - Файл должен быть в `.gitignore`

2. **Проверка:**
   - Всегда проверяй правильность ключей
   - Используй https://jwt.io для проверки JWT токенов
   - Убедись, что anon key содержит `"role":"anon"`

3. **Ошибки:**
   - Если не можешь войти в аккаунт → запроси у пользователя доступ
   - Если ключи не найдены → проверь, что проект существует
   - Если что-то не работает → создай отчёт с проблемами

---

## ✅ ЧЕКЛИСТ

- [ ] Открыл Supabase Dashboard
- [ ] Нашёл проект
- [ ] Скопировал `NEXT_PUBLIC_SUPABASE_URL`
- [ ] Скопировал `NEXT_PUBLIC_SUPABASE_ANON_KEY` (проверил на jwt.io)
- [ ] Скопировал `SUPABASE_SERVICE_ROLE_KEY` (проверил на jwt.io)
- [ ] Открыл OpenAI Dashboard
- [ ] Скопировал `OPENAI_API_KEY`
- [ ] Открыл YooKassa Dashboard (если нужно)
- [ ] Скопировал `YK_SHOP_ID` и `YK_SECRET_KEY` (если нужно)
- [ ] Создал файл `vercel-env-collected.txt`
- [ ] Проверил все переменные
- [ ] Создал отчёт `ENV_COLLECTION_REPORT.md`

---

## 🚀 НАЧНИ С ШАГА 1

**Первое действие:**
1. Открой Supabase Dashboard
2. Найди проект
3. Собери ключи

**После сбора всех переменных:**
- Создай файл для импорта
- Создай отчёт
- Сообщи пользователю, что готово

---

**Собери все переменные автоматически и создай файл для импорта! 🚀**

