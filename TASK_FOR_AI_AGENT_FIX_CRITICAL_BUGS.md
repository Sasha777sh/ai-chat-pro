# 📋 Задача для ИИ-агента: Исправить критические баги

## 🎯 КОНТЕКСТ

После тестирования найдены критические проблемы:
1. ❌ **401 ошибка** в чате (демо-страница)
2. ❌ **500 ошибки** на сервере
3. ❌ **404 ошибка** для `/en` (английская локализация)

## 🚨 КРИТИЧЕСКИЕ БАГИ ДЛЯ ИСПРАВЛЕНИЯ

---

## БАГ #1: Ошибка 401 в демо-чате

### Проблема
При отправке сообщения в демо-чате (`/demo`) появляется ошибка:
```
401 Your authentication token is not from a valid issuer
```

### Причина
Демо-страница использует `/api/demo`, который НЕ требует аутентификации, но возможно:
- Старый код задеплоен на продакшене
- Переменные окружения не настроены
- Проблема с деплоем

### Что проверить

1. **Проверь код `/api/demo/route.ts`:**
   - Файл: `ai-chat-pro/src/app/api/demo/route.ts`
   - Должен быть БЕЗ проверки аутентификации (это демо)
   - Должен просто принимать `message` и возвращать ответ

2. **Проверь переменные окружения на Vercel:**
   - Открой: https://vercel.com/dashboard
   - Выбери проект
   - Settings → Environment Variables
   - Проверь, что есть:
     - `OPENAI_API_KEY` (обязательно!)
     - `NEXT_PUBLIC_SUPABASE_URL`
     - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
     - `SUPABASE_SERVICE_ROLE_KEY`

3. **Проверь деплой:**
   - Последний деплой должен быть после исправлений
   - Если нет — нужно задеплоить заново

### Решение

**Вариант 1: Переменные окружения не настроены**
1. Добавь все переменные в Vercel
2. Перезапусти деплой

**Вариант 2: Старый код задеплоен**
1. Убедись, что код исправлен локально
2. Задеплой заново:
   ```bash
   git add .
   git commit -m "Fix: demo API without auth"
   git push
   ```

**Вариант 3: Проблема с кодом**
1. Проверь файл `ai-chat-pro/src/app/api/demo/route.ts`
2. Убедись, что там НЕТ проверки аутентификации
3. Если есть — убери её

---

## БАГ #2: Ошибки 500 на сервере

### Проблема
500 Internal Server Error при загрузке ресурсов

### Причина
Возможные причины:
- Отсутствует `OPENAI_API_KEY`
- Неправильные переменные окружения
- Ошибки в коде

### Что проверить

1. **Проверь логи Vercel:**
   - Открой: https://vercel.com/dashboard
   - Выбери проект
   - Functions → Logs
   - Посмотри последние ошибки

2. **Проверь переменные окружения:**
   - Все ли переменные на месте?
   - Правильные ли значения?

3. **Проверь код:**
   - Нет ли синтаксических ошибок?
   - Все ли импорты правильные?

### Решение

1. Проверь логи Vercel
2. Исправь ошибки в коде (если есть)
3. Проверь переменные окружения
4. Перезапусти деплой

---

## БАГ #3: 404 для `/en` (английская локализация)

### Проблема
При переходе на `/en` возвращается 404

### Причина
Возможно:
- Нет роутинга для локализации
- Неправильная настройка Next.js

### Что проверить

1. **Проверь структуру папок:**
   - Должна быть: `app/[locale]/` или `app/en/`
   - Или используется middleware для локализации

2. **Проверь `middleware.ts`:**
   - Файл: `ai-chat-pro/src/middleware.ts`
   - Должен обрабатывать `/en` и `/ru`

3. **Проверь роутинг:**
   - Как настроена локализация в Next.js?

### Решение

**Вариант 1: Добавить роутинг для локализации**
1. Создай структуру: `app/[locale]/page.tsx`
2. Настрой middleware для обработки `/en` и `/ru`

**Вариант 2: Использовать query параметры**
1. Вместо `/en` использовать `/?lang=en`
2. Обновить переключатель языка

**Вариант 3: Убрать `/en` роутинг**
1. Оставить только переключатель языка на странице
2. Не использовать `/en` в URL

---

## 📝 ПОШАГОВАЯ ИНСТРУКЦИЯ ПО ИСПРАВЛЕНИЮ

### ШАГ 1: Проверить код локально

1. Открой проект: `ai-chat-pro`
2. Проверь файл: `src/app/api/demo/route.ts`
   - Должен быть БЕЗ проверки аутентификации
   - Должен просто принимать `message` и возвращать ответ
3. Проверь файл: `src/middleware.ts`
   - Как обрабатывается локализация?

### ШАГ 2: Проверить переменные окружения на Vercel

1. Открой: https://vercel.com/dashboard
2. Выбери проект `ai-chat-pro` (или как он называется)
3. Settings → Environment Variables
4. Проверь, что есть:
   - ✅ `OPENAI_API_KEY` (обязательно!)
   - ✅ `NEXT_PUBLIC_SUPABASE_URL`
   - ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - ✅ `SUPABASE_SERVICE_ROLE_KEY`

### ШАГ 3: Проверить логи Vercel

1. В Vercel Dashboard → Functions → Logs
2. Посмотри последние ошибки
3. Запиши все ошибки с точным текстом

### ШАГ 4: Исправить проблемы

1. Если код неправильный — исправь
2. Если переменных нет — добавь в Vercel
3. Если проблема с локализацией — исправь роутинг

### ШАГ 5: Задеплоить исправления

1. Если исправил код локально:
   ```bash
   git add .
   git commit -m "Fix: critical bugs (401, 500, 404)"
   git push
   ```
2. Vercel автоматически задеплоит
3. Дождись завершения деплоя

### ШАГ 6: Проверить исправления

1. Открой: https://chatedem.com/demo
2. Отправь сообщение
3. Проверь, что нет ошибки 401
4. Проверь, что нет ошибок 500
5. Проверь, что `/en` работает (или убран)

---

## 📊 ФОРМА ОТЧЁТА ПО ИСПРАВЛЕНИЮ

После исправления заполни эту форму:

```markdown
# 📋 Отчёт об исправлении критических багов

**Дата:** [дата и время]
**Исправил:** ИИ-агент

---

## ✅ БАГ #1: Ошибка 401 в демо-чате

- **Статус:** [✅ Исправлено / ❌ Не исправлено / ⚠️ Частично]
- **Что сделано:** [опиши, что исправил]
- **Проверено:** [✅ Работает / ❌ Не работает]
- **Примечания:** [если есть]

## ✅ БАГ #2: Ошибки 500 на сервере

- **Статус:** [✅ Исправлено / ❌ Не исправлено / ⚠️ Частично]
- **Что сделано:** [опиши, что исправил]
- **Проверено:** [✅ Работает / ❌ Не работает]
- **Примечания:** [если есть]

## ✅ БАГ #3: 404 для `/en`

- **Статус:** [✅ Исправлено / ❌ Не исправлено / ⚠️ Частично]
- **Что сделано:** [опиши, что исправил]
- **Проверено:** [✅ Работает / ❌ Не работает]
- **Примечания:** [если есть]

---

## 📝 Что было исправлено

1. [список исправлений]

---

## ⚠️ Что осталось

1. [список того, что не удалось исправить]

---

## 🎯 Итог

**Все критические баги исправлены:** [✅ Да / ❌ Нет / ⚠️ Частично]
**Сайт готов к продакшену:** [✅ Да / ❌ Нет / ⚠️ Требуются доработки]
```

---

## 🎯 ИТОГ

**Задача:** Исправить 3 критических бага:
1. Ошибка 401 в демо-чате
2. Ошибки 500 на сервере
3. 404 для `/en`

**Время:** 30-60 минут

**Сложность:** Средняя-Высокая

**Важно:** 
- Проверь переменные окружения на Vercel
- Проверь логи Vercel для понимания ошибок
- Задеплой исправления после изменений

**Начни с ШАГА 1!**

