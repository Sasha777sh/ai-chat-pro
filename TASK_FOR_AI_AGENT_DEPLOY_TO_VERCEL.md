# 🚀 Задача: Задеплоить исправления на Vercel (продакшен)

## 🎯 КОНТЕКСТ

**Важно:** Исправления нужно задеплоить на **продакшен (Vercel)**, а не только тестировать на localhost!

**Проблема:** 
- На localhost всё работает после исправлений
- Но на продакшене (chatedem.com) остаются старые баги
- Нужно задеплоить исправления на Vercel

---

## 📋 ЧТО НУЖНО СДЕЛАТЬ

### ШАГ 1: Проверить, что исправления в коде

Убедись, что исправления уже сделаны:

1. **Файл `src/app/globals.css`:**
   - Должно быть: `@import "tailwindcss";` (не `@tailwind base;`)

2. **Файл `src/app/api/chat/stream/route.ts`:**
   - Должен использовать `NEXT_PUBLIC_SUPABASE_ANON_KEY` для проверки токенов

3. **Файл `src/app/api/demo/route.ts`:**
   - НЕ должен требовать аутентификацию (это демо)

### ШАГ 2: Закоммитить и запушить изменения

```bash
cd /Users/sanecek/tema/ai-chat-pro

# Проверь статус
git status

# Добавь все изменения
git add .

# Закоммить
git commit -m "Fix: Tailwind v4 syntax, 401 auth error, design issues"

# Запушить в репозиторий
git push origin main
```

**Важно:** Если репозиторий не подключен к Vercel, нужно:
1. Создать репозиторий на GitHub
2. Подключить его к Vercel проекту

### ШАГ 3: Проверить подключение Vercel к Git

1. Открой: https://vercel.com/dashboard
2. Выбери проект (скорее всего `ai-chat-pro` или проект с доменом `chatedem.com`)
3. Settings → Git
4. Проверь, что репозиторий подключен
5. Если нет — подключи репозиторий

### ШАГ 4: Vercel автоматически задеплоит

После `git push`:
- Vercel автоматически обнаружит изменения
- Начнёт сборку (build)
- Задеплоит на продакшен

**Проверь:**
1. Vercel Dashboard → Deployments
2. Должен появиться новый деплой
3. Дождись завершения (2-5 минут)

### ШАГ 5: Если автоматический деплой не работает

**Вариант А: Деплой через Vercel CLI**

```bash
# Установи Vercel CLI (если нет)
npm i -g vercel

# Войди в аккаунт
vercel login

# Задеплой на продакшен
cd /Users/sanecek/tema/ai-chat-pro
vercel --prod
```

**Вариант Б: Ручной деплой через Vercel Dashboard**

1. Открой: https://vercel.com/dashboard
2. Выбери проект
3. Deployments → "Redeploy" (если есть последний деплой)
4. Или: Settings → Git → "Redeploy"

### ШАГ 6: Проверить переменные окружения на Vercel

**Критически важно!** Проверь, что все переменные на месте:

1. Vercel Dashboard → Settings → Environment Variables
2. Проверь наличие:
   - ✅ `OPENAI_API_KEY` (обязательно!)
   - ✅ `NEXT_PUBLIC_SUPABASE_URL`
   - ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - ✅ `SUPABASE_SERVICE_ROLE_KEY`
   - ✅ `YK_SHOP_ID` (для YooKassa)
   - ✅ `YK_SECRET_KEY` (для YooKassa)

3. Если чего-то нет — добавь

### ШАГ 7: Проверить логи деплоя

1. Vercel Dashboard → Deployments
2. Выбери последний деплой
3. Посмотри логи сборки (Build Logs)
4. Если есть ошибки — исправь и задеплой снова

### ШАГ 8: Проверить на продакшене

После деплоя проверь:

1. **Дизайн:**
   - Открой: https://chatedem.com
   - Проверь, что стили применяются (тёмный фон, светлый текст)

2. **Демо-чат:**
   - Открой: https://chatedem.com/demo
   - Отправь сообщение
   - Проверь, что нет ошибки 401

3. **Основной чат:**
   - Зарегистрируйся / войди
   - Отправь сообщение
   - Проверь, что работает

---

## 🎯 КРАТКАЯ ИНСТРУКЦИЯ (если всё подключено)

```bash
# 1. Перейди в проект
cd /Users/sanecek/tema/ai-chat-pro

# 2. Проверь изменения
git status

# 3. Закоммить и запушить
git add .
git commit -m "Fix: Tailwind v4, 401 error, design"
git push origin main

# 4. Vercel автоматически задеплоит
# 5. Проверь на https://chatedem.com через 2-5 минут
```

---

## ⚠️ ВАЖНЫЕ МОМЕНТЫ

### 1. Git репозиторий

**Если репозиторий не создан:**
```bash
cd /Users/sanecek/tema/ai-chat-pro
git init
git add .
git commit -m "Initial commit"

# Создай репозиторий на GitHub, затем:
git remote add origin https://github.com/username/ai-chat-pro.git
git push -u origin main
```

### 2. Подключение Vercel к Git

1. Vercel Dashboard → Add New Project
2. Import Git Repository
3. Выбери репозиторий
4. Настрой:
   - Framework: Next.js
   - Root Directory: `./` (или оставь пустым)
   - Build Command: `npm run build`
   - Output Directory: `.next`

### 3. Домен chatedem.com

**Если домен не подключен:**
1. Vercel Dashboard → Settings → Domains
2. Добавь домен: `chatedem.com`
3. Настрой DNS записи (если нужно)

---

## 📊 ФОРМА ОТЧЁТА

После деплоя заполни:

```markdown
# 📋 Отчёт о деплое на Vercel

**Дата:** [дата и время]
**Деплоил:** ИИ-агент

---

## ✅ ШАГ 1: Код исправлен

- [ ] `globals.css` обновлён (Tailwind v4)
- [ ] `api/chat/stream/route.ts` исправлен (401)
- [ ] `api/demo/route.ts` проверен

## ✅ ШАГ 2: Git

- [ ] Изменения закоммичены
- [ ] Изменения запушены в репозиторий
- [ ] Репозиторий подключен к Vercel

## ✅ ШАГ 3: Vercel

- [ ] Деплой запущен автоматически
- [ ] Или деплой выполнен вручную через CLI
- [ ] Логи сборки без ошибок

## ✅ ШАГ 4: Переменные окружения

- [ ] `OPENAI_API_KEY` есть
- [ ] `NEXT_PUBLIC_SUPABASE_URL` есть
- [ ] `NEXT_PUBLIC_SUPABASE_ANON_KEY` есть
- [ ] `SUPABASE_SERVICE_ROLE_KEY` есть
- [ ] Остальные переменные проверены

## ✅ ШАГ 5: Проверка на продакшене

- [ ] Дизайн работает (https://chatedem.com)
- [ ] Демо-чат работает (https://chatedem.com/demo)
- [ ] Нет ошибки 401
- [ ] Нет ошибок 500
- [ ] Основной чат работает (после регистрации)

---

## 🎯 Итог

**Деплой успешен:** [✅ Да / ❌ Нет]
**Сайт работает на продакшене:** [✅ Да / ❌ Нет]
**Остались проблемы:** [список, если есть]
```

---

## 🚨 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Проблема: "Git репозиторий не подключен"
**Решение:** Создай репозиторий на GitHub и подключи к Vercel

### Проблема: "Vercel не деплоит автоматически"
**Решение:** Используй `vercel --prod` через CLI

### Проблема: "Ошибки при сборке"
**Решение:** Проверь логи в Vercel Dashboard → Deployments → Build Logs

### Проблема: "Переменные окружения не работают"
**Решение:** Убедись, что переменные добавлены для всех окружений (Production, Preview, Development)

---

## 🎯 ИТОГ

**Задача:** Задеплоить исправления на Vercel (продакшен)

**Время:** 10-20 минут

**Сложность:** Средняя

**Важно:** 
- Исправления в коде уже есть
- Нужно закоммитить и запушить
- Vercel задеплоит автоматически
- Проверить на продакшене

**Начни с ШАГА 2 — закоммить и запушить изменения!**

