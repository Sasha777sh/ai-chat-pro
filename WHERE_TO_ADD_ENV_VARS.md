# 📍 ГДЕ ДОБАВЛЯТЬ ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ НА VERCEL

## ✅ ПРАВИЛЬНО: На уровне проекта

**Где:** Project Settings → Environment Variables

**Путь:**
1. https://vercel.com/dashboard
2. Найди проект `chatedem.com` (или название проекта)
3. Открой проект
4. Settings (вкладка сверху)
5. Environment Variables (в меню слева)

**Почему правильно:**
- ✅ Каждый проект имеет свои уникальные ключи
- ✅ Безопаснее (изоляция между проектами)
- ✅ Легче управлять (все переменные в одном месте)
- ✅ Можно настроить для разных окружений (Production, Preview, Development)

---

## ❌ НЕПРАВИЛЬНО: На уровне аккаунта/команды

**Где:** Team Settings → Environment Variables

**Почему неправильно:**
- ❌ Все проекты команды будут видеть эти переменные
- ❌ Менее безопасно (общие ключи для всех проектов)
- ❌ Сложнее управлять (непонятно, какой проект использует какие переменные)

**Когда использовать:**
- Только если у тебя несколько проектов с одинаковыми ключами
- Или для общих переменных (например, общий API ключ для всех проектов)

---

## 🎯 КАК ДОБАВИТЬ ПЕРЕМЕННЫЕ НА УРОВНЕ ПРОЕКТА

### Шаг 1: Открой проект
1. https://vercel.com/dashboard
2. Найди проект `chatedem.com`
3. Кликни на проект

### Шаг 2: Открой Settings
1. Вкладка "Settings" (сверху)
2. В меню слева выбери "Environment Variables"

### Шаг 3: Добавь переменные
1. Нажми "Add New"
2. Введи название переменной (например, `NEXT_PUBLIC_SUPABASE_URL`)
3. Введи значение
4. Выбери окружения:
   - ✅ Production (для продакшена)
   - ✅ Preview (для preview деплоев)
   - ✅ Development (для локальной разработки)
5. Нажми "Save"

### Шаг 4: Повтори для всех переменных
Добавь все критичные переменные:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `OPENAI_API_KEY`
- И другие (см. `ALL_ENV_VARIABLES.md`)

---

## 🔍 КАК ПРОВЕРИТЬ, ГДЕ СЕЙЧАС ПЕРЕМЕННЫЕ

### Вариант 1: Проверь на уровне проекта
1. Открой проект на Vercel
2. Settings → Environment Variables
3. Посмотри список переменных

**Если переменные есть здесь** → ✅ Правильно!

**Если переменных нет** → ❌ Нужно добавить!

### Вариант 2: Проверь на уровне команды
1. Vercel Dashboard → Settings (верхний уровень)
2. Team Settings → Environment Variables
3. Посмотри список переменных

**Если переменные только здесь** → ❌ Неправильно! Нужно добавить на уровне проекта!

---

## ⚠️ ВАЖНО

### Переменные на уровне проекта имеют приоритет

Если переменная есть и на уровне проекта, и на уровне команды:
- ✅ Используется переменная с уровня проекта
- ❌ Переменная с уровня команды игнорируется

### Рекомендация

**Всегда добавляй переменные на уровне проекта:**
- Безопаснее
- Понятнее
- Легче управлять

---

## 📋 ЧЕКЛИСТ

- [ ] Открыл проект `chatedem.com` на Vercel
- [ ] Открыл Settings → Environment Variables
- [ ] Проверил наличие всех критичных переменных
- [ ] Если переменных нет → добавил их
- [ ] Выбрал правильные окружения (Production, Preview, Development)
- [ ] Сохранил изменения
- [ ] Передеплоил проект (Redeploy)

---

## 🚀 БЫСТРЫЕ ДЕЙСТВИЯ

**1. Открой проект:**
```
https://vercel.com/dashboard → Проект "chatedem.com" → Settings → Environment Variables
```

**2. Проверь наличие:**
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `OPENAI_API_KEY`

**3. Если нет → добавь:**
- Скопируй значения из `.env.local`
- Добавь на Vercel
- Выбери окружения (Production, Preview)
- Сохрани

**4. Передеплой:**
- Deployments → Redeploy
- Или: `git commit --allow-empty -m "Redeploy" && git push`

---

## 💡 ПОДСКАЗКА

**Если не уверен, где переменные:**
1. Проверь на уровне проекта (Settings → Environment Variables)
2. Если нет → добавь на уровне проекта
3. Если есть на уровне команды → можно оставить, но лучше перенести на уровень проекта

**Главное:** Переменные должны быть доступны для проекта, где бы они ни были. Но лучше всего — на уровне проекта!

---

**Добавь переменные на уровне проекта — это правильный и безопасный способ! ✅**

