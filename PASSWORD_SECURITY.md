# 🔐 Безопасность паролей

## ✅ Что реализовано

### Для пользователей

1. **Валидация пароля при регистрации:**
   - Минимум 8 символов
   - Должна быть хотя бы одна буква (A-Z, a-z)
   - Должна быть хотя бы одна цифра (0-9)
   - Подтверждение пароля (два поля должны совпадать)

2. **Валидация при входе:**
   - Минимум 6 символов
   - Проверка на пустой пароль

3. **Хранение паролей:**
   - Пароли хешируются через Supabase Auth (bcrypt)
   - Пароли никогда не хранятся в открытом виде
   - Используется безопасное хеширование

### Для админки

1. **Двойная проверка доступа:**
   - Проверка сессии Supabase
   - Проверка роли в базе данных (`role = 'admin'`)
   - Дополнительная проверка пользователя

2. **Редирект на логин:**
   - Если не авторизован → `/login?redirect=/admin`
   - После входа перенаправляет обратно в админку

3. **Защита от несанкционированного доступа:**
   - Только пользователи с `role = 'admin` могут войти
   - Автоматический редирект если нет прав

---

## 📝 Требования к паролям

### Регистрация
- **Минимум:** 8 символов
- **Буквы:** хотя бы одна (A-Z или a-z)
- **Цифры:** хотя бы одна (0-9)
- **Подтверждение:** пароли должны совпадать

### Вход
- **Минимум:** 6 символов
- Проверка через Supabase Auth

---

## 🔧 Как изменить требования

### Изменить минимальную длину пароля

**Файл:** `src/app/signup/page.tsx`

Найди функцию `validatePassword` и измени:
```typescript
if (pwd.length < 8) {  // Измени 8 на нужное значение
  return 'Пароль должен быть не менее 8 символов';
}
```

### Добавить дополнительные требования

В функции `validatePassword` можно добавить:
```typescript
// Требование заглавной буквы
if (!/[A-Z]/.test(pwd)) {
  return 'Пароль должен содержать хотя бы одну заглавную букву';
}

// Требование спецсимвола
if (!/[!@#$%^&*]/.test(pwd)) {
  return 'Пароль должен содержать хотя бы один спецсимвол';
}
```

---

## 🛡️ Безопасность админки

### Как сделать пользователя админом

1. **Через SQL в Supabase:**
```sql
UPDATE profiles 
SET role = 'admin' 
WHERE email = 'твой@email.com';
```

2. **Через админ-панель:**
   - Войди как админ
   - Введи email пользователя
   - Нажми "Назначить"

### Проверка доступа

Админка проверяет:
1. ✅ Есть ли активная сессия
2. ✅ Есть ли роль `admin` в таблице `profiles`
3. ✅ Совпадает ли ID пользователя с сессией

Если любая проверка не проходит → доступ запрещён.

---

## ✅ Готово

Система паролей настроена и работает:
- ✅ Валидация при регистрации
- ✅ Валидация при входе
- ✅ Защита админки
- ✅ Безопасное хеширование через Supabase

