# 🚨 СРОЧНАЯ ЗАДАЧА: Задеплоить исправление ошибки 401

## 🎯 КОНТЕКСТ

**Проблема:** Ошибка 401 "Your authentication token is not from a valid issuer" блокирует чат на продакшене (chatedem.com).

**Статус:** Исправление уже есть в коде, но НЕ задеплоено на продакшен!

**Критичность:** 🔴 КРИТИЧНО — основная функция сайта не работает!

---

## ✅ ЧТО УЖЕ ИСПРАВЛЕНО В КОДЕ

Исправление уже сделано в файле `src/app/api/chat/stream/route.ts`:

```typescript
// ✅ ПРАВИЛЬНО (исправлено):
const supabaseAuth = createClient(supabaseUrl, supabaseAnonKey); // Использует anon key
const { data: { user }, error: authError } = await supabaseAuth.auth.getUser(token);

// ❌ НЕПРАВИЛЬНО (было раньше):
// const supabaseAuth = createClient(supabaseUrl, supabaseServiceKey); // Использовало service key
```

**Проблема:** Этот код исправлен локально, но на продакшене (Vercel) всё ещё старый код!

---

## 🚀 ЧТО НУЖНО СДЕЛАТЬ СЕЙЧАС

### ШАГ 1: Проверить, что исправления в Git

```bash
cd /Users/sanecek/tema/ai-chat-pro

# Проверь статус
git status

# Если есть незакоммиченные изменения:
git add .
git commit -m "Fix: 401 auth error - use anon key for token verification"
git push origin main
```

**Важно:** Если репозиторий не подключен к Vercel, нужно:
1. Создать репозиторий на GitHub
2. Подключить его к Vercel проекту

### ШАГ 2: Проверить подключение Vercel к Git

1. Открой: https://vercel.com/dashboard
2. Найди проект с доменом `chatedem.com`
3. Settings → Git
4. Проверь:
   - Подключён ли репозиторий?
   - Правильная ли ветка (обычно `main`)?

**Если репозиторий не подключён:**
1. Settings → Git → Connect Git Repository
2. Выбери репозиторий
3. Настрой деплой

### ШАГ 3: Задеплоить исправления

**Вариант А: Автоматический деплой (если Git подключён)**

После `git push` Vercel автоматически задеплоит изменения.

**Проверь:**
1. Vercel Dashboard → Deployments
2. Должен появиться новый деплой
3. Дождись завершения (2-5 минут)

**Вариант Б: Ручной деплой через Vercel CLI**

```bash
# Установи Vercel CLI (если нет)
npm i -g vercel

# Войди в аккаунт
vercel login

# Задеплой на продакшен
cd /Users/sanecek/tema/ai-chat-pro
vercel --prod
```

**Вариант В: Ручной деплой через Vercel Dashboard**

1. Vercel Dashboard → Deployments
2. Найди последний деплой
3. Нажми "Redeploy" (если есть)
4. Или: Settings → Git → "Redeploy"

### ШАГ 4: Проверить переменные окружения на Vercel

**КРИТИЧЕСКИ ВАЖНО!** Проверь, что переменные правильные:

1. Vercel Dashboard → Settings → Environment Variables
2. Проверь наличие:
   - ✅ `NEXT_PUBLIC_SUPABASE_ANON_KEY` — **ОБЯЗАТЕЛЬНО!**
   - ✅ `SUPABASE_SERVICE_ROLE_KEY` — **ОБЯЗАТЕЛЬНО!**
   - ✅ `NEXT_PUBLIC_SUPABASE_URL` — **ОБЯЗАТЕЛЬНО!**
   - ✅ `OPENAI_API_KEY` — **ОБЯЗАТЕЛЬНО!**

3. Проверь значения:
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` должен быть **anon/public key** из Supabase
   - `SUPABASE_SERVICE_ROLE_KEY` должен быть **service_role key** из Supabase
   - Они должны быть **разными**!

4. Если переменных нет или они неправильные:
   - Добавь/исправь в Vercel
   - Перезапусти деплой

### ШАГ 5: Проверить логи деплоя

1. Vercel Dashboard → Deployments
2. Выбери последний деплой
3. Посмотри Build Logs
4. Проверь, что нет ошибок

**Если есть ошибки:**
- Запиши текст ошибки
- Исправь проблему
- Задеплой снова

### ШАГ 6: Проверить на продакшене

После деплоя (подожди 2-5 минут):

1. **Открой:** https://chatedem.com
2. **Войди** в систему (или зарегистрируйся)
3. **Открой чат**
4. **Отправь сообщение**
5. **Проверь:**
   - ✅ Нет ошибки 401?
   - ✅ Приходит ответ от ИИ?
   - ✅ Чат работает?

**Если всё ещё ошибка 401:**
- Проверь логи Vercel (Functions → Logs)
- Проверь, что переменные окружения правильные
- Проверь, что деплой завершился успешно

---

## 🔍 ДОПОЛНИТЕЛЬНАЯ ПРОВЕРКА

### Проверка 1: Правильность переменных

1. Открой Supabase Dashboard: https://supabase.com/dashboard
2. Выбери проект
3. Settings → API
4. Скопируй:
   - **Project URL** → должен совпадать с `NEXT_PUBLIC_SUPABASE_URL`
   - **anon/public key** → должен совпадать с `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role key** → должен совпадать с `SUPABASE_SERVICE_ROLE_KEY`

5. Сравни с переменными в Vercel
6. Если не совпадают — исправь в Vercel

### Проверка 2: Логи на Vercel

1. Vercel Dashboard → Functions → Logs
2. Открой чат на сайте
3. Отправь сообщение
4. Посмотри логи:
   - Есть ли ошибка 401?
   - Есть ли другие ошибки?
   - Что пишется в консоли?

### Проверка 3: Проверка кода на продакшене

Если есть доступ к коду на продакшене, проверь:

1. Открой файл `/api/chat/stream/route.ts` на продакшене
2. Проверь строку ~70:
   - Должно быть: `createClient(supabaseUrl, supabaseAnonKey)`
   - НЕ должно быть: `createClient(supabaseUrl, supabaseServiceKey)`

---

## 📊 ФОРМА ОТЧЁТА

После выполнения заполни:

```markdown
# 📋 Отчёт о деплое исправления 401

**Дата:** [дата и время]
**Деплоил:** ИИ-агент

---

## ✅ ШАГ 1: Git

- [ ] Изменения закоммичены
- [ ] Изменения запушены в репозиторий
- [ ] Репозиторий подключён к Vercel

## ✅ ШАГ 2: Vercel

- [ ] Деплой запущен
- [ ] Деплой завершён успешно
- [ ] Логи без ошибок

## ✅ ШАГ 3: Переменные окружения

- [ ] `NEXT_PUBLIC_SUPABASE_ANON_KEY` есть и правильный
- [ ] `SUPABASE_SERVICE_ROLE_KEY` есть и правильный
- [ ] `NEXT_PUBLIC_SUPABASE_URL` есть и правильный
- [ ] `OPENAI_API_KEY` есть

## ✅ ШАГ 4: Проверка на продакшене

- [ ] Открыл https://chatedem.com
- [ ] Вошёл в систему
- [ ] Открыл чат
- [ ] Отправил сообщение
- [ ] **НЕТ ошибки 401** ✅
- [ ] Приходит ответ от ИИ ✅
- [ ] Чат работает ✅

---

## 🎯 Итог

**Ошибка 401 исправлена:** [✅ Да / ❌ Нет]
**Чат работает:** [✅ Да / ❌ Нет]
**Проблемы:** [список, если есть]
```

---

## 🚨 ЕСЛИ НЕ РАБОТАЕТ

### Проблема: "Git репозиторий не подключён"
**Решение:**
1. Создай репозиторий на GitHub
2. Подключи к Vercel: Settings → Git → Connect Git Repository

### Проблема: "Деплой не запускается"
**Решение:**
1. Используй Vercel CLI: `vercel --prod`
2. Или: Vercel Dashboard → Deployments → Redeploy

### Проблема: "Всё ещё ошибка 401"
**Решение:**
1. Проверь переменные окружения (должны совпадать с Supabase)
2. Проверь логи Vercel (Functions → Logs)
3. Убедись, что деплой завершился успешно
4. Очисти кеш браузера (Ctrl+Shift+R)

### Проблема: "Переменные окружения неправильные"
**Решение:**
1. Открой Supabase Dashboard → Settings → API
2. Скопируй правильные ключи
3. Обнови в Vercel: Settings → Environment Variables
4. Перезапусти деплой

---

## 🎯 ИТОГ

**Задача:** Задеплоить исправление ошибки 401 на продакшен

**Время:** 15-30 минут

**Критичность:** 🔴 КРИТИЧНО

**Важно:**
- Исправление уже в коде
- Нужно закоммитить и запушить
- Vercel задеплоит автоматически
- Проверить переменные окружения
- Протестировать на продакшене

**Начни с ШАГА 1 — закоммить и запушить изменения!**

