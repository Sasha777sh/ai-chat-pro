# 📥 Импорт переменных окружения в Vercel

## 🚀 БЫСТРАЯ КОМАНДА

**Выполни в терминале:**
```bash
cd /Users/sanecek/tema/ai-chat-pro
./get-vercel-env.sh
```

**Или:**
```bash
cd /Users/sanecek/tema/ai-chat-pro && ./get-vercel-env.sh
```

**Результат:** Выведет все переменные в формате для импорта

---

## 📋 ЧТО ДЕЛАТЬ С РЕЗУЛЬТАТОМ

### Вариант 1: Импорт через файл

**1. Сохрани вывод в файл:**
```bash
./get-vercel-env.sh > vercel-env.txt
```

**2. Открой Vercel:**
- https://vercel.com/dashboard
- Найди проект
- Settings → Environment Variables

**3. Импортируй:**
- Нажми "Import" (если есть)
- Или скопируй содержимое файла и вставь вручную

### Вариант 2: Копирование напрямую

**1. Выполни команду:**
```bash
./get-vercel-env.sh
```

**2. Скопируй весь вывод** (все переменные)

**3. Открой Vercel:**
- https://vercel.com/dashboard
- Найди проект
- Settings → Environment Variables

**4. Вставь переменные:**
- Нажми "Add New" для каждой переменной
- Или используй "Import" (если доступно)

---

## ✅ ЧЕКЛИСТ

- [ ] Выполнил команду `./get-vercel-env.sh`
- [ ] Скопировал все переменные
- [ ] Открыл Vercel Dashboard
- [ ] Нашёл проект (ai-chat-pro или chatedem.com)
- [ ] Открыл Settings → Environment Variables
- [ ] Добавил все переменные
- [ ] Выбрал окружения: Production, Preview
- [ ] Сохранил изменения
- [ ] Передеплоил проект (Redeploy)

---

## 📝 ПЕРЕМЕННЫЕ, КОТОРЫЕ БУДУТ ВЫВЕДЕНЫ

**Критические (обязательные):**
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `OPENAI_API_KEY`

**Важные (для оплаты):**
- `YK_SHOP_ID` (если есть)
- `YK_SECRET_KEY` (если есть)
- `NEXT_PUBLIC_APP_URL` (автоматически заменит localhost на chatedem.com)

**Опциональные:**
- `CRYPTO_IPN_SECRET` (если есть)
- `MANUAL_PAYMENT_*` (если есть)
- `SUPPORT_EMAIL` (если есть)

---

## ⚠️ ВАЖНО

**После импорта:**
1. Проверь, что `NEXT_PUBLIC_APP_URL` = `https://chatedem.com` (не localhost!)
2. Проверь, что все переменные добавлены
3. Выбери правильные окружения (Production, Preview)
4. **ОБЯЗАТЕЛЬНО передеплой проект!**

---

## 🔧 ЕСЛИ КОМАНДА НЕ РАБОТАЕТ

**Проверь:**
```bash
# Права на выполнение
chmod +x get-vercel-env.sh

# Существование файла
ls -la .env.local

# Выполни команду
./get-vercel-env.sh
```

**Или используй готовый файл:**
- Открой `vercel-env-import.txt`
- Скопируй переменные (без комментариев)
- Вставь в Vercel

---

**Выполни команду и импортируй переменные в Vercel! 🚀**

