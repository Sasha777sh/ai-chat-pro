# 🤖 КОМАНДА ДЛЯ ИИ-АГЕНТА: Собрать переменные окружения

## 🎯 ВЫПОЛНИ ЭТУ ЗАДАЧУ

**Задача:** Автоматически собрать все переменные окружения из дашбордов и создать файл для импорта в Vercel.

**Файл с полной инструкцией:** `TASK_FOR_AI_AGENT_COLLECT_ENV_VARS.md`

---

## 🚀 БЫСТРЫЙ СТАРТ

### 1. Открой Supabase Dashboard

**Команда:**
```
browser_navigate("https://supabase.com/dashboard")
```

**Что делать:**
1. Войди в аккаунт (если нужно)
2. Найди проект с URL: `https://jgnnrdrqzcwnhuuvhlfo.supabase.co`
3. Открой: Settings → API
4. Скопируй:
   - Project URL → `NEXT_PUBLIC_SUPABASE_URL`
   - anon/public key → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - service_role key → `SUPABASE_SERVICE_ROLE_KEY`

**Проверка ключей:**
- Открой https://jwt.io
- Вставь ключ
- Проверь поле `"role"`:
  - Anon key → должно быть `"anon"`
  - Service role key → должно быть `"service_role"`

---

### 2. Открой OpenAI Dashboard

**Команда:**
```
browser_navigate("https://platform.openai.com/api-keys")
```

**Что делать:**
1. Войди в аккаунт (если нужно)
2. Найди существующий API ключ или создай новый
3. Скопируй ключ → `OPENAI_API_KEY`

**Проверка:**
- Ключ должен начинаться с `sk-proj-` или `sk-`

---

### 3. Открой YooKassa Dashboard (опционально)

**Команда:**
```
browser_navigate("https://yookassa.ru/developers")
```

**Что делать:**
1. Войди в аккаунт (если нужно)
2. Найди Shop ID и Secret Key
3. Скопируй:
   - Shop ID → `YK_SHOP_ID`
   - Secret Key → `YK_SECRET_KEY`

---

### 4. Создай файл для импорта

**Создай файл:** `vercel-env-collected.txt`

**Содержимое:**
```bash
NEXT_PUBLIC_SUPABASE_URL=<скопированный URL>
NEXT_PUBLIC_SUPABASE_ANON_KEY=<скопированный anon key>
SUPABASE_SERVICE_ROLE_KEY=<скопированный service role key>
OPENAI_API_KEY=<скопированный API ключ>
NEXT_PUBLIC_APP_URL=https://chatedem.com
```

**Если есть YooKassa:**
```bash
YK_SHOP_ID=<скопированный Shop ID>
YK_SECRET_KEY=<скопированный Secret Key>
```

---

### 5. Проверь и создай отчёт

**Проверь:**
- Все переменные не пустые
- Anon key содержит `"role":"anon"` (проверь на jwt.io)
- Service role key содержит `"role":"service_role"` (проверь на jwt.io)
- `NEXT_PUBLIC_APP_URL` = `https://chatedem.com`

**Создай отчёт:** `ENV_COLLECTION_REPORT.md`

**Содержимое:**
```markdown
# Отчёт о сборе переменных окружения

## ✅ Собранные переменные:
- NEXT_PUBLIC_SUPABASE_URL: ✅ Собрано
- NEXT_PUBLIC_SUPABASE_ANON_KEY: ✅ Собрано (проверено на jwt.io)
- SUPABASE_SERVICE_ROLE_KEY: ✅ Собрано (проверено на jwt.io)
- OPENAI_API_KEY: ✅ Собрано
- NEXT_PUBLIC_APP_URL: ✅ Установлено (https://chatedem.com)

## 📁 Файл для импорта:
- `vercel-env-collected.txt` - готов для импорта в Vercel

## 📝 Инструкция по импорту:
1. Открой Vercel Dashboard
2. Найди проект
3. Settings → Environment Variables
4. Импортируй файл `vercel-env-collected.txt`
5. Выбери окружения: Production, Preview
6. Сохрани и передеплой проект
```

---

## 🔧 ИНСТРУМЕНТЫ

**Используй браузерные инструменты:**
- `browser_navigate(url)` - открыть страницу
- `browser_snapshot()` - получить содержимое страницы
- `browser_click(element, ref)` - кликнуть на элемент
- `browser_type(element, ref, text)` - ввести текст
- `browser_evaluate(function)` - выполнить JavaScript

**Пример:**
```javascript
// Открыть страницу
browser_navigate("https://supabase.com/dashboard")

// Получить содержимое
browser_snapshot()

// Найти и кликнуть
browser_click(element="Settings", ref="...")

// Выполнить JavaScript для получения ключей
browser_evaluate(function="() => {
  // Найти элементы с ключами на странице
  const url = document.querySelector('[data-testid=\"project-url\"]')?.textContent;
  const anonKey = document.querySelector('[data-testid=\"anon-key\"]')?.textContent;
  const serviceKey = document.querySelector('[data-testid=\"service-key\"]')?.textContent;
  return { url, anonKey, serviceKey };
}")
```

---

## ⚠️ ВАЖНО

1. **Безопасность:**
   - Не публикуй ключи в отчёте
   - Храни их только в `vercel-env-collected.txt`
   - Добавь файл в `.gitignore`

2. **Проверка:**
   - Всегда проверяй ключи на jwt.io
   - Убедись, что anon key содержит `"role":"anon"`
   - Убедись, что service role key содержит `"role":"service_role"`

3. **Ошибки:**
   - Если не можешь войти → запроси у пользователя доступ
   - Если ключи не найдены → проверь, что проект существует
   - Если что-то не работает → создай отчёт с проблемами

---

## ✅ ЧЕКЛИСТ

- [ ] Открыл Supabase Dashboard
- [ ] Собрал `NEXT_PUBLIC_SUPABASE_URL`
- [ ] Собрал `NEXT_PUBLIC_SUPABASE_ANON_KEY` (проверил на jwt.io)
- [ ] Собрал `SUPABASE_SERVICE_ROLE_KEY` (проверил на jwt.io)
- [ ] Открыл OpenAI Dashboard
- [ ] Собрал `OPENAI_API_KEY`
- [ ] Открыл YooKassa Dashboard (если нужно)
- [ ] Собрал `YK_SHOP_ID` и `YK_SECRET_KEY` (если нужно)
- [ ] Создал файл `vercel-env-collected.txt`
- [ ] Проверил все переменные
- [ ] Создал отчёт `ENV_COLLECTION_REPORT.md`

---

## 🚀 НАЧНИ СЕЙЧАС

**Первая команда:**
```
browser_navigate("https://supabase.com/dashboard")
```

**После сбора всех переменных:**
- Создай файл `vercel-env-collected.txt`
- Создай отчёт `ENV_COLLECTION_REPORT.md`
- Сообщи пользователю, что готово

---

**Выполни эту задачу и собери все переменные автоматически! 🚀**

