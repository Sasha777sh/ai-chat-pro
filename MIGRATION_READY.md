# ✅ Миграция Supabase — Готова к выполнению

## 📊 Статус

**✅ Готово к выполнению**

Все файлы подготовлены, инструкции созданы, код проверен.

---

## 📁 Подготовленные файлы

1. **`SUPABASE_MIGRATION.sql`** — SQL-код миграции
2. **`AI_AGENT_SUPABASE_MIGRATION.md`** — Полная инструкция для выполнения
3. **`MIGRATION_READY.md`** — Этот файл (чеклист готовности)

---

## ✅ Что включает миграция

### 1. Поддержка голосов

- ✅ Колонка `voice_id` в `chat_sessions`
- ✅ 5 голосов: `live`, `mirror`, `child`, `shadow`, `sage`
- ✅ Значение по умолчанию: `live`

### 2. Обновление тарифов

- ✅ Тарифы в `profiles`: `free`, `basic`, `plus`, `pro`
- ✅ Автоматическая установка `free` для существующих пользователей

### 3. Таблица подписок

- ✅ Создание `billing_subscriptions`
- ✅ Поддержка планов: `basic`, `plus`, `pro`
- ✅ Хранение статуса, дат, ID подписок

### 4. Безопасность

- ✅ Row Level Security (RLS) включен
- ✅ Политики: пользователи видят только свои подписки

### 5. Автоматизация

- ✅ Триггер для `updated_at`
- ✅ Автоматическое обновление при изменении подписки

---

## 🚀 Как выполнить миграцию

### Вариант 1: По инструкции (рекомендуется)

1. Открой `AI_AGENT_SUPABASE_MIGRATION.md`
2. Следуй пошаговой инструкции
3. Выполни SQL-код в Supabase SQL Editor

### Вариант 2: Быстро

1. Открой Supabase Dashboard → SQL Editor
2. Скопируй код из `SUPABASE_MIGRATION.sql`
3. Вставь в SQL Editor
4. Нажми "Run" или Ctrl+Enter

---

## ✅ Чеклист перед выполнением

- [ ] Supabase проект создан
- [ ] Есть доступ к SQL Editor
- [ ] Базовые таблицы существуют (`chat_sessions`, `profiles`)
- [ ] SQL-код скопирован
- [ ] Готов выполнить миграцию

---

## 🔍 Проверка после выполнения

После выполнения миграции проверь:

### 1. Таблица `chat_sessions`

```sql
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'chat_sessions' 
AND column_name = 'voice_id';
```

**Ожидается:** колонка `voice_id` типа `text`

### 2. Таблица `profiles`

```sql
SELECT DISTINCT subscription_tier 
FROM profiles;
```

**Ожидается:** `free`, `basic`, `plus`, `pro`

### 3. Таблица `billing_subscriptions`

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_name = 'billing_subscriptions';
```

**Ожидается:** таблица существует

### 4. RLS политики

```sql
SELECT policyname 
FROM pg_policies 
WHERE tablename = 'billing_subscriptions';
```

**Ожидается:** 2 политики (SELECT, UPDATE)

---

## ⚠️ Возможные проблемы

### Проблема: "relation does not exist"

**Решение:** Сначала выполни базовые миграции (001_init.sql, 002_add_admin.sql)

### Проблема: "column already exists"

**Решение:** Это нормально, миграция использует `IF NOT EXISTS`

### Проблема: "permission denied"

**Решение:** Убедись, что используешь правильный проект и есть права администратора

---

## 📝 Отчёт после выполнения

После успешного выполнения напиши:

```
✅ Миграция выполнена успешно

Проверено:
- ✅ Колонка voice_id добавлена в chat_sessions
- ✅ Тарифы обновлены в profiles
- ✅ Таблица billing_subscriptions создана
- ✅ RLS политики настроены
- ✅ Триггер created

Готово к использованию!
```

---

## 🎯 Следующие шаги после миграции

1. **Проверь переменные окружения:**

   ```env
   NEXT_PUBLIC_SUPABASE_URL=...
   NEXT_PUBLIC_SUPABASE_ANON_KEY=...
   SUPABASE_SERVICE_ROLE_KEY=...
   ```

2. **Протестируй API:**
   - Создай тестового пользователя
   - Попробуй создать сессию чата с голосом
   - Проверь работу биллинга

3. **Готово к деплою!**

---

## 📊 Время выполнения

- **Подготовка:** ✅ Завершена
- **Выполнение:** ~1-2 минуты
- **Проверка:** ~2-3 минуты
- **Итого:** ~5 минут

---

**Статус:** ✅ **Готово к выполнению**

**Риск:** Низкий (используется IF NOT EXISTS, безопасные операции)

**Сложность:** Простая (пошаговая инструкция готова)

