# 🚨 Статус: Ошибка 401 на продакшене

## 📊 Текущая ситуация

**Проблема:** Ошибка 401 "Your authentication token is not from a valid issuer" блокирует чат на https://chatedem.com

**Статус исправления:**
- ✅ **Код исправлен** локально
- ❌ **НЕ задеплоено** на продакшен (Vercel)

**Критичность:** 🔴 КРИТИЧНО — основная функция сайта не работает!

---

## ✅ Что уже исправлено

Исправление в файле `src/app/api/chat/stream/route.ts`:

**Было (неправильно):**
```typescript
const supabaseAuth = createClient(supabaseUrl, supabaseServiceKey); // ❌
```

**Стало (правильно):**
```typescript
const supabaseAuth = createClient(supabaseUrl, supabaseAnonKey); // ✅
```

**Также исправлено в:**
- `src/app/api/yookassa/checkout/route.ts`
- `src/app/api/crypto/checkout/route.ts`
- `src/app/api/payment/manual/route.ts`

---

## 🚀 Что нужно сделать

### Вариант 1: Если Git подключён к Vercel

```bash
cd /Users/sanecek/tema/ai-chat-pro
git add .
git commit -m "Fix: 401 auth error - use anon key for token verification"
git push origin main
```

Vercel автоматически задеплоит через 2-5 минут.

### Вариант 2: Если Git не подключён

1. Создай репозиторий на GitHub
2. Подключи к Vercel: Settings → Git → Connect Git Repository
3. Запуши код
4. Vercel задеплоит автоматически

### Вариант 3: Ручной деплой через CLI

```bash
npm i -g vercel
vercel login
cd /Users/sanecek/tema/ai-chat-pro
vercel --prod
```

---

## ⚠️ Важно проверить

### Переменные окружения на Vercel

1. Vercel Dashboard → Settings → Environment Variables
2. Проверь наличие:
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` — **ОБЯЗАТЕЛЬНО!**
   - `SUPABASE_SERVICE_ROLE_KEY` — **ОБЯЗАТЕЛЬНО!**
   - `NEXT_PUBLIC_SUPABASE_URL` — **ОБЯЗАТЕЛЬНО!**
   - `OPENAI_API_KEY` — **ОБЯЗАТЕЛЬНО!**

3. Проверь значения:
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = anon/public key из Supabase
   - `SUPABASE_SERVICE_ROLE_KEY` = service_role key из Supabase
   - Они должны быть **разными**!

---

## 📋 Чек-лист

- [ ] Код исправлен локально ✅
- [ ] Изменения закоммичены
- [ ] Изменения запушены в Git
- [ ] Репозиторий подключён к Vercel
- [ ] Деплой запущен на Vercel
- [ ] Деплой завершён успешно
- [ ] Переменные окружения проверены
- [ ] Протестировано на https://chatedem.com
- [ ] Ошибка 401 исправлена ✅

---

## 📝 Задачи для ИИ-агента

1. **Срочно:** `TASK_FOR_AI_AGENT_URGENT_DEPLOY_401_FIX.md` — задеплоить исправление
2. **Потом:** `TASK_FOR_AI_AGENT_FULL_AUDIT.md` — полный аудит системы

---

## 🎯 Итог

**Проблема:** Ошибка 401 блокирует чат  
**Решение:** Исправление в коде, нужно задеплоить  
**Действие:** Задеплоить на Vercel  
**Время:** 15-30 минут

