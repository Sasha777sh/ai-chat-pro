# 📋 Задача для ИИ-агента: Проверить исправление ошибки 401

## 🎯 Что нужно сделать

Проверить, что ошибка 401 "Your authentication token is not from a valid issuer" исправлена.

---

## 📝 Пошаговая инструкция

### ШАГ 1: Проверить переменные окружения

1. Открой файл `.env.local` в корне проекта `ai-chat-pro`
2. Проверь, что есть все переменные:

```
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxx...
SUPABASE_SERVICE_ROLE_KEY=eyJxxx...
OPENAI_API_KEY=sk-xxx...
```

**Важно:**
- `NEXT_PUBLIC_SUPABASE_URL` должен совпадать с URL твоего проекта в Supabase
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` — это anon key (публичный ключ)
- `SUPABASE_SERVICE_ROLE_KEY` — это service role key (секретный ключ)

3. Если переменных нет — добавь их из Supabase Dashboard:
   - Settings → API → Project URL и API keys

### ШАГ 2: Перезапустить dev-сервер

1. Если dev-сервер запущен — останови его (Ctrl+C в терминале)
2. Запусти заново:

```bash
cd /Users/sanecek/tema/ai-chat-pro
npm run dev
```

3. Дождись сообщения: **"Ready"** или **"Local: http://localhost:3000"**

### ШАГ 3: Проверить работу регистрации

1. Открой браузер
2. Перейди на: **https://chatedem.com/signup**
3. Зарегистрируй нового пользователя:
   - Email: `test@example.com` (или любой другой)
   - Password: минимум 8 символов, буквы и цифры
4. Нажми **"Зарегистрироваться"**
5. **Проверь:** Должна быть успешная регистрация, без ошибок 401

### ШАГ 4: Проверить работу входа

1. Если автоматически не вошёл — перейди на: **https://chatedem.com/login**
2. Введи email и пароль
3. Нажми **"Войти"**
4. **Проверь:** Должен быть успешный вход, без ошибок 401

### ШАГ 5: Проверить работу чата

1. После входа найди кнопку для начала чата
2. Отправь сообщение (например: "Привет")
3. **Проверь:**
   - ИИ отвечает?
   - Нет ошибок 401 в консоли браузера (F12 → Console)?
   - Нет ошибок в терминале dev-сервера?

### ШАГ 6: Проверить консоль браузера

1. Открой консоль браузера (F12 → Console)
2. Проверь, что нет ошибок:
   - ❌ `401 Unauthorized`
   - ❌ `Your authentication token is not from a valid issuer`
   - ✅ Должны быть только обычные логи (если есть)

### ШАГ 7: Проверить логи сервера

1. Посмотри в терминал, где запущен `npm run dev`
2. Проверь, что нет ошибок:
   - ❌ `Auth error: ...`
   - ❌ `401`
   - ✅ Должны быть только обычные логи запросов

---

## ✅ Что должно работать

После исправления должно работать:

- ✅ Регистрация без ошибок 401
- ✅ Вход без ошибок 401
- ✅ Чат отвечает на сообщения
- ✅ Нет ошибок в консоли браузера
- ✅ Нет ошибок в логах сервера

---

## ⚠️ Если ошибка осталась

### Ошибка: "Your authentication token is not from a valid issuer"

**Причина:** URL Supabase не совпадает с токеном

**Решение:**
1. Проверь `NEXT_PUBLIC_SUPABASE_URL` в `.env.local`
2. Убедись, что URL совпадает с URL проекта в Supabase Dashboard
3. Перезапусти dev-сервер

### Ошибка: "Missing environment variable"

**Причина:** Нет переменных окружения

**Решение:**
1. Добавь все переменные в `.env.local`
2. Перезапусти dev-сервер

### Ошибка: "Invalid API key"

**Причина:** Неверный anon key или service role key

**Решение:**
1. Проверь ключи в Supabase Dashboard → Settings → API
2. Скопируй правильные ключи в `.env.local`
3. Перезапусти dev-сервер

---

## 📊 Отчёт после проверки

После проверки напиши:

```
✅ Проверка завершена

Переменные окружения: [Все есть / Отсутствуют: ...]
Dev-сервер: [Запущен / Ошибка]
Регистрация: [Работает / Ошибка: ...]
Вход: [Работает / Ошибка: ...]
Чат: [Работает / Ошибка: ...]
Консоль браузера: [Нет ошибок / Ошибки: ...]
Логи сервера: [Нет ошибок / Ошибки: ...]

Ошибка 401 исправлена: [Да / Нет]
```

---

## 🎯 Итог

**Задача:** Проверить, что ошибка 401 исправлена после изменений в коде

**Время:** 5-10 минут

**Сложность:** Простая

**Важно:** Перезапусти dev-сервер после проверки переменных окружения!

