# 🔧 Исправление проблемы с деплоем в Vercel

## Проблема
В Vercel есть проект, который деплоится на старый сайт (chatdem), а обновления деплоятся на другой сайт.

## Причины
1. **Несколько проектов в Vercel** — возможно, есть старый проект "chatdem" и новый "ai-chat-pro"
2. **Неправильно настроены домены** — production и preview деплои на разные домены
3. **Неправильный `NEXT_PUBLIC_APP_URL`** — переменная окружения указывает на старый домен

## Решение

### Вариант 1: Объединить проекты (рекомендуется)

1. **Зайти в Vercel Dashboard:**
   - https://vercel.com/dashboard

2. **Найти оба проекта:**
   - Старый проект (chatdem)
   - Новый проект (ai-chat-pro)

3. **Проверить домены:**
   - В каждом проекте: Settings → Domains
   - Посмотреть, какие домены привязаны

4. **Выбрать основной проект:**
   - Оставить проект `ai-chat-pro` как основной
   - Удалить или переименовать старый проект `chatdem`

5. **Перенести домен:**
   - В проекте `ai-chat-pro`: Settings → Domains → Add Domain
   - Добавить домен, который был на `chatdem`
   - Удалить домен из старого проекта

### Вариант 2: Использовать один проект

1. **Удалить старый проект:**
   - Vercel Dashboard → проект `chatdem` → Settings → Delete Project

2. **Настроить домены в `ai-chat-pro`:**
   - Settings → Domains
   - Добавить нужный домен (chatdem или другой)

3. **Обновить переменные окружения:**
   ```env
   NEXT_PUBLIC_APP_URL=https://your-actual-domain.com
   ```

### Вариант 3: Настроить Production Branch

1. **В проекте `ai-chat-pro`:**
   - Settings → Git
   - Production Branch: `main` или `master`

2. **Проверить деплои:**
   - Deployments → посмотреть, какие ветки деплоятся
   - Production деплои должны идти на основной домен

## Пошаговая инструкция

### Шаг 1: Проверить текущее состояние

```bash
# Проверить, какой проект привязан
cat .vercel/project.json

# Должно быть:
# {"projectId":"prj_A3mk0085At3UHDg6RqdNdev8VKrs","orgId":"team_XZCt3aMCcwpyadyNgM3CyPa1","projectName":"ai-chat-pro"}
```

### Шаг 2: Проверить домены в Vercel

1. Открыть: https://vercel.com/dashboard
2. Выбрать проект `ai-chat-pro`
3. Settings → Domains
4. Посмотреть список доменов

### Шаг 3: Обновить переменные окружения

В Vercel Dashboard → проект `ai-chat-pro` → Settings → Environment Variables:

```env
NEXT_PUBLIC_APP_URL=https://your-actual-domain.com
```

**Важно:** Заменить `your-actual-domain.com` на реальный домен, который должен быть основным.

### Шаг 4: Передеплоить

```bash
# Локально
vercel --prod

# Или через Git push
git push origin main
```

### Шаг 5: Проверить webhook URL

После обновления домена обновить webhook в YooKassa:
- YooKassa Dashboard → Webhooks
- URL: `https://your-actual-domain.com/api/yookassa/webhook`

## Проверка

1. ✅ Все деплои идут на один домен
2. ✅ `NEXT_PUBLIC_APP_URL` указывает на правильный домен
3. ✅ Webhook URL в YooKassa обновлён
4. ✅ Production деплои работают корректно

## Если проблема осталась

1. **Проверить, нет ли нескольких проектов:**
   - Vercel Dashboard → все проекты
   - Убедиться, что только один проект активен

2. **Проверить Git интеграцию:**
   - Settings → Git
   - Убедиться, что подключён правильный репозиторий

3. **Очистить кеш:**
   - Deployments → выбрать последний деплой → Redeploy

4. **Проверить ветки:**
   - Убедиться, что production branch = `main` или `master`

---

**Текущий проект:** `ai-chat-pro`  
**Project ID:** `prj_A3mk0085At3UHDg6RqdNdev8VKrs`

